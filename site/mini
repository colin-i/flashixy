mini () {
	b=$(date +%s)$1
	html-minifier-terser $1 --collapse-whitespace --remove-comments --minify-js true --minify-css true -o $b
	cat $b | wc -l
	mv $b $2
	if [ -z "${no_remove}" ]; then
		rm $1
	fi
}
